---
type: map
mapping:
    "unpack":
        type: seq
        sequence:
          - type: map
            mapping:
                "source": { type: str, required: true }
                "sourcefs": { type: str }
                "destination": { type: str }
