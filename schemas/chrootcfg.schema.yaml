---
type: map
mapping:
    requirements:
          type: seq
          sequence:
            - type: map
              mapping:
                "name": { type: str, required: true }
                "mode": { type: str, required: true }
    keyrings:
        type: seq
        sequence:
          - type: str
